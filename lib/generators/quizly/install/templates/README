# Post-Quizly Installation

Run your migrations:

```bash
rails db:migrate
```

## Configuration

Edit `config/initializers/quizly.rb` to customize settings:

```ruby
Quizly.configure do |config|
  # config.default_locale = :en
  # config.shuffle_questions = true
  # config.shuffle_choices = true
end
```